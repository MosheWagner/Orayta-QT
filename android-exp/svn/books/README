
הספרים כאן הינם תחת רשיון Creative Commons או GFDL.


אם הספרים הורדו בנפרד מקוד המקור, יש להריץ כאן:
sh GenMakeFile.sh
ולאחר מכן(כמשתמש על):
make install
אם זה לא עובד צריך להעתיק את הספרים ידנית לתיקיה:
/usr/share/Orayta/Books
הפקודה הבאה אמורה לעשות זאת אוטומטית (להריץ מתוך התיקיה Books אליה הורדתם את הספרים, כמשתמש על):
cp -r -f ./ /usr/share/Orayta/


If the books were downloaded separately, you should run in their folder:
sh GenMakeFile.sh
and then (as root):
make install

if this doesn't work you should copy all the books manually to the directory:
/usr/share/Orayta/Books
the following command is supposed to do this:
cp -r -f ./ /usr/share/Orayta/
